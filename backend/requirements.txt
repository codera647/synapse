# DEPRECATED — requirements are now split for clean GPU deployment:
#
#   requirements-base.txt   CPU-safe: API + workers (install everywhere)
#   requirements-gpu.txt    GPU-only: torch / doclayout-yolo / surya / transformers
#
# Install:
#   pip install -r requirements-base.txt
#   pip install -r requirements-gpu.txt    # on the GPU host (after installing torch)
#
# This file is kept only so existing scripts that reference it still resolve.
-r requirements-base.txt
